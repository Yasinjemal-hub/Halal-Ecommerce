# ─── Environment Configuration ───────────────────────────
NODE_ENV=development

# ─── Server Configuration ─────────────────────────────────
PORT=5000

# ─── Database Configuration ───────────────────────────────
# MongoDB Atlas (recommended) or local MongoDB
# MONGO_ATLAS_URI=mongodb+srv://username:password@cluster.mongodb.net/halal-ecommerce?retryWrites=true&w=majority
# MONGO_URI=mongodb://localhost:27017/halal-ecommerce

# ─── JWT Configuration ────────────────────────────────────
# Generate strong secrets (min 32 chars): openssl rand -base64 48
JWT_SECRET=your-super-secret-jwt-access-token-key-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-jwt-refresh-token-key-min-32-chars
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
ACCESS_TOKEN_MAX_AGE_MS=900000

# ─── Client Configuration ─────────────────────────────────
# Frontend development URL
CLIENT_URL=http://localhost:3000
# Additional allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# ─── Email Configuration (Gmail SMTP) ─────────────────────
# 1. Enable 2FA on Gmail
# 2. Generate App Password: https://myaccount.google.com/apppasswords
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_gmail_app_password
EMAIL_FROM="Halal E-Commerce <your_email@gmail.com>"

# ─── Payment Configuration (Optional) ─────────────────────
# STRIPE_API_KEY=your_stripe_publishable_key
# STRIPE_SECRET_KEY=your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=your_stripe_webhook_secret
# PAYPAL_CLIENT_ID=your_paypal_client_id
# PAYPAL_CLIENT_SECRET=your_paypal_client_secret

# ─── Image Search API Keys (for scripts/generateProductImages.js) ──
# Sign up at https://www.pexels.com/api/ for a free Pexels API key
# PEXELS_API_KEY=your_pexels_api_key
# Sign up at https://pixabay.com/api/docs/ for a free Pixabay API key
# PIXABAY_API_KEY=your_pixabay_api_key

# ─── Cloudinary Configuration (for file uploads and image generation) ──
# Sign up at https://cloudinary.com - free tier available
# Required for scripts/generateProductImages.js:
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# ─── File Upload Configuration ────────────────────────────
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# ─── Image Configuration ───────────────────────────────────
# Base URL for generated product images (defaults to placehold.co public CDN)
# Use this to point to your own image service if needed
# For local uploads in production, set to your backend domain:
# IMAGE_BASE_URL=https://your-backend.onrender.com
IMAGE_BASE_URL=https://placehold.co/800x800

# ─── Rate Limiting ────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100